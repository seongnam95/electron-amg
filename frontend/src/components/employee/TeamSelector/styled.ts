import styled from 'styled-components';

export const TeamSelectorStyled = styled.div`
  .team-label {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-size: 2rem;
    cursor: pointer;
  }

  .color-bar {
    width: 0.8rem;
    height: 1.8rem;
    border-radius: 0.2rem;
  }
`;
