import styled from 'styled-components';

export const AttendanceStatusStyled = styled.div`
  height: 100%;

  display: flex;
  flex-direction: column;
  gap: 2.4rem;
  align-items: center;
  justify-content: space-between;

  .chart-wrap {
    position: relative;
    max-width: 14rem;
    max-height: 14rem;
  }
`;
