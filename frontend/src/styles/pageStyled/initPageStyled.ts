import styled from 'styled-components';

export const InitPageStyled = styled.div`
  position: relative;
  height: 100%;

  .container {
    position: absolute;
    left: 50%;
    top: 6%;
    transform: translateX(-50%);
  }
`;
