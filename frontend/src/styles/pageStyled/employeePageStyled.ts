import styled from 'styled-components';

export const EmployeePageStyled = styled.div`
  position: relative;
  height: 100%;

  .EmployeeTable {
    display: flex;
    flex-direction: column;
    gap: 1.4rem;
  }
`;
