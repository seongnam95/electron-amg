import styled from "styled-components";

export const DraftContractViewStyled = styled.div`
  padding: 0 2rem;

  .form-wrap {
    display: flex;
    flex-direction: column;
    gap: 4.2rem;
  }

  .create-btn {
    position: fixed;
    bottom: 0;
    left: 0;
  }

  .row-wrap {
    display: flex;
    gap: 1.2rem;
  }
`;
